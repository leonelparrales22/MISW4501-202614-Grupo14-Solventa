# Candidate configuration. Copy as deployment/ec2.env and adjust only for a new AWS run.
CB_FAILURE_THRESHOLD=3
CB_RECOVERY_WINDOW=5s
CB_HALF_OPEN_SUCCESS_THRESHOLD=3
RISK_PROFILE_CACHE_TTL=24h
EXTERNAL_TIMEOUT=700ms

# Workload generated inside the EC2 instance.
VUS=10
DURATION=20s
